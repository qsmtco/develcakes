# transport/ — SPEC-05 R1 gateway strip, decision #1: the transport core
# (connection lifecycle, reconnect/backoff, framing) retained and cleaned
# for reuse by the future Telegram transport; OpenClaw protocol and
# identity/auth removed (architecture ruling #7).
#
# DORMANT in Sub-Phase 1: zero callers. SP2 repoints send sites onto it.
