# render — the R2 Phase A pipeline: markdown → HTML → sanitize.
#
# Package marker + docstring only (SPEC-05 transport/ pattern). Pure-function
# stages: render/sanitize.py is the fail-closed nh3 wrapper; html.py and
# syntax_html.py land in later sub-phases.
